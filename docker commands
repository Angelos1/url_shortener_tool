docker stop $(docker ps -a -q)

docker rm $(docker ps -aq)

docker rmi $(docker images -a -q)





docker exec -it url_shortener-app-1 bash
docker exec -it mongodb bash
mongosh "mongodb://localhost:27017" --username camara --password camara --authenticationDatabase admin
use url_shortener
db.urls.find()
db.urls.deleteMany({})

python main.py --expand 'camarasoft.com/gsiKna'
python main.py --minify 'https://www.linkedin.com/jobs/search/?cur&rentJobId=36835896434444osdhcjfwbhoiufwefoihoiewrhfoihssssdsdcds'


COPY ./airflow /opt/miniconda3/lib/python3.11/site-packages/airflow/example_dags

    volumes:
      - mongodb_data:/data/db
volumes:
  mongodb_data:

docker compose up
docker exec -it url_shortener-app-1 bash
python main.py --minify



from pymongo import MongoClient
import os

MONGO_URI = os.getenv("MONGO_URI")

# Establishing connection
try:
    myclient = MongoClient(MONGO_URI)
    print(myclient.server_info())
except Exception as e:
    print("Could not connect to MongoDB")
    print(e)

# creating the database
db = myclient["url_shortener"]

# creating the collection
col = db["urls"]




docker compose up -d
docker exec -it url_shortener_tool bash
python main.py --minify 'https://www.linkedin.com/jobs/search/?cur&rentJobId=36835896434444osdhcjfwbhoiufwefoihoiewrhfoihssssdsdcds'
python main.py --expand 'camarasoft.com/gsiKna'
